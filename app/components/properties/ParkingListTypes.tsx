export type ParkingType = {
    id: string;
    title: string;
    image_url: string;
    price_per_hour: number;
    is_favorite: boolean;
    check_in: string;
    check_out: string;
    user_id: string;
};
